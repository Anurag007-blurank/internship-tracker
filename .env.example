PORT=3000
JWT_SECRET=replace_this_with_a_long_random_string

# Optional — if left blank, the app logs reminders to the console instead of emailing.
# To use real Gmail sending: enable 2FA on your Google account, then create an
# "App Password" at https://myaccount.google.com/apppasswords and use it below.
EMAIL_USER=
EMAIL_PASS=
